diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index a90ab6f..3459bbd 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -331,6 +331,7 @@ message(STATUS "Python modules will be installed to: " ${Python_site_packages})
 message(STATUS "Python libraries will be installed to: " ${Python_arch_packages})
 
 if (BUILD_PY_LIB)
+  link_libraries(${Boost_LIBRARY_DIRS})
   # this makes the ocl Python module
   add_library(
     ocl 
